spring:
  datasource:
    # ?????? ?? ?? (?? ?? pineapple_db ??)
    url: jdbc:mysql://localhost:3306/pineapple_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    # MySQL ?? ? ??? ??? ?? (???? root)
    username: root
    # MySQL ?? ? ??? ????
    password: britz1413*
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      # ddl-auto: update (??? ???? ???? DB ???? ???? ??)
      # ? ?? ??! ????? validate ?? none? ??.
      ddl-auto: update
    # ???? SQL ??? ???? ??
    show-sql: true
    properties:
      hibernate: